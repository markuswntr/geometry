os:
    - linux
    - osx
language: generic
sudo: required
dist: trusty
env:
    - SWIFT_VERSION=swift-4.1-DEVELOPMENT-SNAPSHOT-2018-02-01-a
install:
    - eval "$(curl -sL https://swiftenv.fuller.li/install.sh)"
script:
    - swift build
    - swift build -c release
    - swift test
